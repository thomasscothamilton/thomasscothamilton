apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thomasscothamilton.fullname" . }}-test-connection"
  labels:
    {{- include "thomasscothamilton.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thomasscothamilton.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
